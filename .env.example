# For local development, copy this file to `.env.development.local` (gitignored)
# and set your private, localhost-restricted dev key. It overrides `.env` during `just run`.
FF7EC_GOOGLE_API_KEY=your-localhost-restricted-dev-key
