.parcel-cache
.spago
.psc-ide-port
.purs-repl
.spec-results
.psci_modules
!.gitignore
!.github

output
generated-docs
bower_components
node_modules

package-lock.json
*.lock
!spago.lock

dist/
prod/*
staging/*

tmp/*

tsconfig.tsbuildinfo

# Local env overrides (e.g. private dev API key). `.env` and `.env.example` stay tracked.
.env.local
.env.*.local
